@model FeatureIndexViewModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
    int count = 0;

}
<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <div class="bs-example">
                    <div id="Feature-grid-container">
                        @* Sayfa ilk açıldığında loading/skeleton görünsün diye *@
                        <partial name="_FeatureContentPartial" model="Model" />
                    </div>

                
                    <a href="/Admin/Feature/CreateFeature/"
                       class="btn btn-primary mt-3 @(Model.IsLoading ? "disabled" : "")">
                        Yeni Feature Girişi
                    </a>


                </div>
            </div>
        </div>
    </div>
</section>
@section Scripts {
    <script>
        $(document).ready(function () {
        var url = '@Url.Action("GetFeatureListPartial", "Feature", new { area = "Admin" })';
            // Sayfa açılır açılmaz isteği başlat
            $.get(url, function (data) {
                // Gelen hazır HTML paketini konteynerin içine bas
                $("#Feature-grid-container").html(data);

                // Yükleme bittiği için butonu aktif et (isteğe bağlı)
                $(".btn-primary").removeClass("disabled");
            });
        });
    </script>
    }
@* <section class="content container-fluid">
    <div class="row">

        <div class="col-md-12">
            <div class="chart-box">
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Öne Çıkan Alan Adı</th>
                                <th>Sil</th>
                                <th>Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <th scope="row">@count</th>
                                    <td>@item.Title</td>
                                    <td><a href="/Admin/Feature/DeleteFeature/@item.FeatureId" class="btn btn-danger">Sil</a></td>
                                    <td><a href="/Admin/Feature/UpdateFeature/@item.FeatureId" class="btn btn-success">Güncelle</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Admin/Feature/CreateFeature/" class="btn btn-primary">Yeni Öne Çıkan Alan Girişi</a>
                </div>
            </div>
        </div>



    </div>
</section>
 *@


