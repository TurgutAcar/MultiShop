@model CategoryIndexViewModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

@* <section class="content container-fluid">
    <div class="row">

        <div class="col-md-12">
            <div class="chart-box">
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Kategori Adı</th>
                                <th>Sil</th>
                                <th>Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                    <tr>
                                        <th scope="row">@count</th>
                                        <td>@item.CategoryName</td>
                                        <td><a href="/Admin/Category/DeleteCategory/@item.CategoryId" class="btn btn-danger">Sil</a></td>
                                        <td><a href="/Admin/Category/UpdateCategory/@item.CategoryId" class="btn btn-success">Güncelle</a></td>
                                    </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Admin/Category/CreateCategory/" class="btn btn-primary">Yeni Kategori Girişi</a>
                </div>
            </div>
        </div>



    </div>
</section>
 *@
 <section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <div class="bs-example">
                    <div id="category-grid-container">
                        @* Sayfa ilk açıldığında loading/skeleton görünsün diye *@
                        <partial name="_CategoryContentPartial" model="Model" />
                    </div>

                 @*    @if (Model.IsLoading)
                    {
                         <div class="alert alert-info mb-3">
                            Sistem yoğunluğu nedeniyle içerikler yükleniyor…
                        </div> 

                        <partial name="_SkeletonBrandTable" />
                    }
                    else if (!Model.Brands.Any())
                    {
                        <div class="alert alert-secondary">
                            Henüz marka bulunmuyor.
                        </div>
                    }
                    else
                    {
                        <partial name="_BrandTable" model="Model.Brands" />
                    } *@
                    <a href="/Admin/Category/CreateCategory/"
                       class="btn btn-primary mt-3 @(Model.IsLoading ? "disabled" : "")">
                        Yeni Kategori Girişi
                    </a>
                  

                </div>
            </div>
        </div>
    </div>
</section>
@section Scripts {
    <script>
        $(document).ready(function () {
        var url = '@Url.Action("GetCategoryListPartial", "Category", new { area = "Admin" })';
            // Sayfa açılır açılmaz isteği başlat
            $.get(url, function (data) {
                // Gelen hazır HTML paketini konteynerin içine bas
                $("#category-grid-container").html(data);

                // Yükleme bittiği için butonu aktif et (isteğe bağlı)
                $(".btn-primary").removeClass("disabled");
            });
        });
    </script>
    }