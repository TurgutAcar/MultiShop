@* @model List<ResultBrandDto>

@{
    int count = 0;
}

<table class="table table-hover align-middle">
    <thead class="table-light">
        <tr>
            <th>#</th>
            <th>Marka</th>
            <th>Görsel</th>
            <th class="text-center">İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            count++;
            <tr>
                <td>@count</td>
                <td>@item.BrandName</td>
                <td>
                    <img src="@item.ImageUrl"
                         class="rounded"
                         style="width:40px;height:40px;object-fit:cover;" />
                </td>
                <td class="text-center">
                    <a href="/Admin/Brand/UpdateBrand/@item.BrandId"
                       class="btn btn-sm btn-success me-1">
                        <i class="fa fa-edit"></i>
                    </a>

                    <button class="btn btn-sm btn-danger"
                            onclick="confirmDelete('@item.BrandId','@item.BrandName')">
                        <i class="fa fa-trash"></i>
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>
 *@

<div class="skeleton-table">
    @for (int i = 0; i < 5; i++)
    {
        <div class="skeleton-row d-flex align-items-center mb-2">
            <div class="skeleton-box me-2" style="width:40px"></div>
            <div class="skeleton-line flex-grow-1"></div>
            <div class="skeleton-box ms-2" style="width:80px"></div>
        </div>
    }
</div>
